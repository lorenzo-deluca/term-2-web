version: '3.8'
services:
  term2web:
    build: .
    container_name: term2web
    privileged: true
    volumes:
      - /dev:/dev
      - ./data:/app/data
    ports:
      - "5001:5001"
      - "8080:8080"
      - "3333:3333"
    restart: unless-stopped